'use strict';

const stream = async (fastify, {request}) => {
// generate random room id
    // create that room
    // emit the socket.id of the client

    const msg = "hi "
    return msg;
};

module.exports = {stream};
